<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="premium.manager.mapper.SysMenuMapper">

    <select id="findAll" resultType="premium.model.entity.system.SysMenu">
        select id, parent_id, title, component, sort_value, status, create_time, update_time, is_deleted
        from sys_menu
        where is_deleted=0
        order by sort_value;
    </select>


    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into sys_menu (
                              id, parent_id, title, component, sort_value, status
        ) VALUES (
                  #{id}, #{parentId}, #{title}, #{component}, #{sortValue}, #{status}
                         )
    </insert>

    <update id="update">
        update sys_menu set
        <if test="parentId != null and parentId != ''">
            parent_id = #{parentId},
        </if>
        <if test="title != null and title != ''">
            title = #{title},
        </if>
        <if test="component != null and component != ''">
            component = #{component},
        </if>
        <if test="sortValue != null">
            sort_value = #{sortValue},
        </if>
        <if test="status != null">
            status = #{status},
        </if>
        update_time = now()
        where
        id=#{id}
    </update>

    <select id="selectCountById" resultType="java.lang.Integer">
        select COUNT(*) from sys_menu where parent_id=#{id} and is_deleted=0
    </select>

    <update id="delete">
        update sys_menu set is_deleted=1 where id=#{id}
    </update>

</mapper>