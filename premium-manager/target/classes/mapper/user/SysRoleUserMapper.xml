<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="premium.manager.mapper.SysRoleUserMapper">

    <!--  根据用户id删除数据  -->
    <delete id="deleteRoleByUserId">
        delete from sys_user_role where user_id=#{userId}
    </delete>

    <!--  assign roles by userid  -->
    <insert id="doAssign">
        insert into sys_user_role(user_id, role_id, create_time, update_time, is_deleted)
        VALUES(#{userId}, #{roleId}, now(), now(), 0)
    </insert>

    <!--  find roles by userId  -->
    <select id="findRolesByUserId" resultType="long">
        select role_id from sys_user_role where is_deleted=0 and user_id=#{userId}
    </select>

</mapper>