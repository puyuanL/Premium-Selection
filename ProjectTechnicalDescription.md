# 项目技术说明



## 一、管理端实现













## 二、前台系统实现



#### 2.1 用户注册 & 短信验证码

使用阿里云短信服务（三网短信），购买短信验证码使用额度，代码中使用 AppCode 请求验证码发送。

流程：

> ​	用户输入手机号后，点击 “获取验证码” ，前端向后端发送获取验证码请求，后端接收到请求，随机生成一个数字验证码，将用户手机号作为key，验证码作为 value 保存在 Redis 中。
>
> ​	用户输入完所有注册信息后，前端将用户信息打包为请求体，向后端发送注册请求，后端：
> ​	首先，在数据库中查找用户名，若已有带用户名，则抛出异常。
> ​	其次，根据用户手机号从 Redis 中取出验证码，并于请求体中的验证码进行对比，若验证码不一致，则抛出异常。
> ​	否则，将请求体信息打包放入新建的用户信息对象中，并将对象保存到数据库中（密码用 md5 进行加密）
> ​	最后，删除 Redis 中 key 为用户手机号的键值，并向前端返回请求成功的结果。



#### 2.2 用户登录 & 网关过滤器

**用户登录：**用户点击 “加入购物车” 等操作时，提示需要登陆。

> ​	用户输入用户名和密码，前端将用户的输入信息打包进入请求体，并像后端发送请求，后端接到请求后，根据用户名从数据库中查询用户信息（其中包括用户加密后的密码）
>
> ​	若没有查到用户信息，则抛出异常，提示不存在用户；否则，将用户输入的密码进行加密，并与用户的输入数据库中的用户密码进行比对。若比对相同，则生成一个token，并将token作为key，用户信息（转换成String）作为value，存放到 Redis 中。

 **<u>以上为初级版本</u>** 



**网关过滤器 + 用户登录：**网页中的某些功能需要进行登录判断

> ​	设置拦截器，对所有传入的（“ /api/** ”）请求进行拦截，并使用 filter 进行过滤，判断是否需要拦截
>
> ​	过滤器：设置一个特定的请求路径，如：“/\*\*/auth/\*\*” 表示需要对登录状态进行验证，通过全局过滤器进行实现（AntPathMatcher 进行路径匹配），如果请求路径不符合上述格式，则直接放行；否则，校验用户是否登录。
>
> ​	校验方法：从请求头中获取 token ，如果 token 为空，则拦截请求，并返回用户未登录的错误信息；否则，在 Redis 中查找出 key 为 token 的键值，value 即为用户的登录信息。判断用户登录信息是否为空，若为空则用户未登录或登录过期，拦截该请求；否则，用户状态为已登录，放行请求，并**将用户信息存储在 ThreadLocal 中**。



#### 2.3 购物车

使用 Redis 存储购物车中的数据，Redis支持多种数据类型，采用 Hash 类型（key - field - value 模式）

**添加购物车：**

> ​	首先从 ThreadLocal 中获取用户信息，根据用户的 userId 从 Redis 中获取购物车关于本件商品的信息（key: userId ; field: skuId ; value: sku information CartInfo）
>
> ​	若本件商品已存在，则相加购买数量；
> ​	否则，需要使用 Nacos + OpenFeign 获取商品 Sku 信息后，再根据上述信息将商品添加到 Redis 中。
>
> 补充（**Nacos + OpenFeign 获取商品 Sku 信息 流程**）：
> 	1 -- 在商品模块中编写 “获取商品sku信息” 接口
> 	2 -- 在client客户端模块中定义 “商品Feign客户端接口” （注意获取 Nacos 注册中心的服务名称）
> 	3 -- 在购物车模块中引入上述定义的接口，使购物车模块可以远程调用 Product 模块服务（@EnableFeignClients 开启远程调用）

**购物车列表：**

> ​	从Redis中根据 用户id 获取购物车信息，若返回列表为空，则返回空列表；否则，将 Redis 数据转换成正确格式的列表格式返回。



#### 2.4 订单模块

**订单结算：** 

使用 Redis + OpenFeign 远程调用，实现获取订单选中商品及金额

> ​	在购物车模块中实现获取选中商品列表的接口（列表中包含所有 “选中” == 1 的商品 sku 列表），并将该接口注册到客户端模块中。在交易结算界面，前端会调用交易结算接口，通过远程调用上述接口，实现获取选中商品列表和商品总价值。
>
> 
>
> **Bug：** **service-order微服务 调用 service-cart微服务 的时候，是通过 OpenFeign 进行调用，OpenFeign在调用的时候会丢失请求头，导致service-cart微服务是获取不到当前登录用户的信息** 
> **解决方案：使用 feign 拦截器拦截请求，获取 token，重新传递token** 
>
> ​	定义拦截器，从请求头中获取Token，并将token重新放到 RequestTemplate 中，将	该拦截器包装成注解，并在 service-order微服务 的启动类上添加该注解。

**订单生成——订单信息获取：** 

> ​	前端向后端发送带有订单详细信息（包含：送货地址、运费、订单商品明细）的请求。后端根据这些信息，生成订单编号，并将信息封装存入数据库。随后远程调用 “service-cart”微服务 清除购物车中选中商品的信息。最后将订单号返回给前端。
>
> ​	前端根据后端返回的订单号，再次向后端发送请求，获取订单信息，用于在支付界面显示订单金额。

**立即购买：**

> ​	前端向后端发送带有商品 skuId 的请求，后端 "service-order"微服务 根据 skuId 向 “service-product” 微服务进行远程调用，获取商品的具体sku信息，之后根据商品信息封装为 “订单结算信息”，返回给前端。

**订单超时取消：**





#### 2.5 订单支付

**第一阶段（premium服务调用支付宝服务）：**

> ​	用户点击确认支付，前端向后端发送请求，后端会将支付记录保存到支付记录表。之后，调用支付宝服务接口，传递参数。在调用支付宝服务接口，返回form表单页面（支付页面）

**第二阶段（支付宝服务调用premium服务）：**

> ​	支付宝支付完成后，调用本地接口（配置）



**保存支付记录：** 

> 逻辑：查询支付信息数据，如果已经已经存在了就不用进行保存 (一个订单支付失败以后可以继续支付)
>
> 实现流程：
> 	查询数据库中是否存在支付记录，若存在则不进行支付记录保存；若不存在，则根据订单编号远程调用 ”order-service微服务“ ，获取订单信息，并根据订单信息封装数据，将数据保存到数据库中。



**支付流程（调用支付宝接口进行支付）：**

> ​	首先，调用保存支付记录接口。之后，调用支付宝支付接口（同步回调、异步回调），并接收支付接口返回的响应。获取响应信息，若支付成功，则向前端返回相应；若支付失败，则抛出异常。
>
> ​	支付成功后，前端会调用发送请求，更新订单支付信息、订单支付状态、商品销量以及库存信息



## 三、项目部署

1、在docker中安装mysql、redis、nacos

2、更改配置文件application.yml，并将application-dev.yml中的内容在nacos创建配置

3、使用Harbor创建镜像仓库，在docker中配置并部署服务





## 四、项目优化

**1、防止库存超卖（锁方案）**

（1）MySQL 方案：直接更改数据库，使用数据库自带的 InnoDB 排他锁，在 SQL 中判断库存是否足够。
			优点：数据强一直性
			缺点：并发能力一般，适合对一致性要求高的场景

（2）Redis 方案：把库存与加载到 Redis 用 SETNX 加锁 或 Lua 脚本扣减库存，然后异步同步到 MySQL 。
			优点：性能超高，能抗住高并发
			缺点：可能存在短暂的数据不一致



本项目采用（2）Redis方案，整体逻辑方案如下：

> 用户下单 → 预占Redis库存 → 生成订单 → 等待支付
>     ├── 支付成功 → 扣减数据库库存 → 订单发货
>     └── 超时/取消 → 释放Redis预占库存 → 订单关闭
>
> ​	用户点击下单，前端向后端发送请求，生成订单请求。后端将商品库存信息加载到Redis中，并在Redis中预占库存（若商品信息已在Redis中，直接预占库存，库存不足则抛出异常）。
> ​	若支付成功，则扣减Redis中库存（删除预占库存 & 并扣减真实库存），使用 RabbitMQ 异步修改数据库信息，并且远程调用 service微服务修改订单信息为支付完成。
>
> 
>
> **Redis锁优化：**
>
> ​	带有重试机制（**指数退避 + 随机抖动**）的 Redis锁获取，防止大量请求在同一时间点集中重试，减少 Redis 的瞬时压力。
> ​	加锁时，会生成线程的 UUID，并将其作为Redis的value。解锁时，会校验线程传入的UUID并对比value进行校验（使用Lua脚本保证操作的原子性）



高并发调用测试（暂时移除支付功能，方便数据测试）—— <u>同时进行1000条请求，成功（999条，1条抛出系统繁忙异常）</u> 



**2、sentinel 限流（仅对提交订单和支付进行限流）**

在项目中引入sentinel核心依赖、nacos作为sentinel数据源依赖，并在nacos中配置sentinel流控规则，

> 阈值类型：QPS
> 流控模式：直接
> 流控效果：排队等待
> 超时时间：1000ms

在需要进行流控的请求上添加@SentinelResourse注解，并编写blockHandler作为兜底函数，兜底函数直接返回 ”系统繁忙“ 的请求结果



**3、微服务？redis？分布式**









